setup node with npm `npm init`
and do the thing then if i want to use modules add this in `package.json`

```json
{
  "type": "module"
}
```


in prod use `npm install --production` dev package not include